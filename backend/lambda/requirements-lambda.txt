# Minimal runtime for API Lambda (FastAPI + ASGI in Lambda). Keep in sync with imports used
# on the app startup / request path (add packages if the API routes require more).

boto3==1.40.3
botocore==1.40.3

fastapi==0.115.12
mangum==0.19.0
pydantic==2.10.3
pydantic-settings==2.8.1
python-multipart==0.0.20
PyJWT[crypto]==2.10.1
httpx==0.28.1
pypdf==5.4.0
python-docx==1.1.2
structlog==24.4.0
prometheus-client==0.21.1
beautifulsoup4==4.12.3
orjson==3.11.8
uvicorn==0.34.0

# LangGraph / LLM (required by imports in the API surface)
langgraph==1.1.9
langchain-core>=1.3.0
langchain-openai>=0.3.0

# Observability
langfuse>=4.5.0

# Tenacity and friends used in services
tenacity==9.1.4

# Aurora / PostgreSQL (psycopg3; used when DB_BACKEND=postgres)
psycopg[binary]>=3.2.0
