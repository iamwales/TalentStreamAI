# The root module already declares `backend "s3" { }` in `main.tf`.
# Do not add a second backend block. Use:
#   terraform init -backend-config=...   (see `scripts/deploy.sh` and root `README.md`)
# or, for a one-time local run:
#   terraform init -reconfigure -backend=false
